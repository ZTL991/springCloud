eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@127.0.0.1:8000/eureka/
spring:
  application:
    name: client3
server:
  port: 8002
  management:
    security:
      enabled: false
feign:
  hystrix:
    enabled: true